his project implements a simple ETL (Extract, Transform, Load) pipeline for RetailMart Pvt. Ltd.

The pipeline reads raw sales data from multiple CSV files, cleans and transforms the data, and loads the final dataset into a SQLite database for reporting and analysis.
